export interface GenerateReserve {
  user_id: string;   // ID del usuario
  field_id: string;  // ID de la cancha
  time: string;      // Hora de la reserva (ej: "9:00")
  date: string;      // Fecha en formato YYYY-MM-DD (ej: "2025-06-05")
}
